# Working principles

- Build and test the complete automation system on this Mac first. Move it to the second Mac only after local acceptance. Assume only one Mac operates at a time. Repeat machine-specific checks after transfer.

- Apply [the ASD-STE100 skill](tools/ste/SKILL.md) to documents outside `data/`. Use its strict mode for procedures. Use its prose mode for explanations. Preserve facts, uncertainty, commands and requirements. Run `python3 check_docs.py` after document changes. Review advisory findings and meaning manually. The checker does not certify official dictionary compliance.
- Do not apply the writing skill to any file under `data/`. Do not rewrite existing data files for style. Future data files also have this exception.

- Large-file exception: depth-chart JSON snapshots go under ignored `data/nflverse/local/`, not Git. After cloning, `python3 setup_repo.py --season 2026` restores them through the central nflverse client with a matching collection manifest and normal cache/quota policies.

- SEPTEMBER 10 UPDATE: user explicitly authorized committing all project data to Git and assumes only one Mac operates at a time. Track `data/` including snapshots and provider ledgers. Keep `.env` and credentials excluded. Use normal Git commit/push/pull for handoff. No separate data-transfer system, host-isolation system, clean-history export, or concurrent-Mac coordination is required. This supersedes older local-only data/publication instructions below. Do not push unless requested.

- CURRENT STATUS (supersedes historical draft instructions below): the real draft is complete, all 196 league picks and our 14 players reconciled. Watcher stopped. No pending submission. Do not resume drafting or start another mock. Read `docs/PROGRESS.md` and `docs/REAL_DRAFT_RESULT.md` on resume. Week 1 lineup work is still outstanding.
- DATA FREEZE RELEASED: user explicitly authorized release after completion. `python3 draft_session.py release` confirmed completion through central Sleeper access and released the guard at 2026-09-09T03:46:42Z. No FantasyPros/nflverse refresh was made during release. Historical frozen files remain evidence, not current weekly advice. Future collection must retain normal central-client cache/quota policies.
- Treat informational questions as curiosity, not instructions to change strategy. Distinguish numerical advice from agent overrides and explain the evidence and uncertainty.
- Publication: keep `.env` and all `data/` local. Generated data existed in Git history. Ignoring/untracking it does not remove older copies. Do not push or rewrite history without authorization. Curated result documentation may contain personal league identifiers. Review before publication.

- FINAL USER-CONFIRMED ROOM: https://sleeper.com/draft/nfl/1400628785413394432 . The user will do no more rehearsals. Default `draft_now.py` successfully read this actual draft at 2026-09-08 23:06:48 UTC: pre_draft, zero picks, seat 13, next pick 13. Frozen-board and live-format validation passed. Use this room tonight, without `--mock` / `--allow-mock`. Do not start further mocks. This preflight validates the current configuration, not future API/browser uptime or uninterrupted operation.

- TONIGHT'S OPERATING UPDATE: the third mock was partially executed, then stopped by the user after four supervised picks. Read `docs/MOCK_3_ADVANCED.md` and `docs/DRAFT_RUNBOOK.md` before tonight's draft and after any interruption. Search players explicitly in the virtualized list. Add queue entries sequentially and verify each visible update. Never assume a batch of clicks persisted. Keep commentary/observations compact and maintain the queue between turns. Test central Sleeper access before starting a clock: restricted execution returned 503 while the same command succeeded with network permission. Do not automatically label a 503 a provider outage. Live API/browser agreement still gates selection. Full-draft reliability, compaction recovery, late-round coverage, quantified API lag and a durable per-pick strategy audit remain unverified or unfinished. Stopping the watcher does not pause Sleeper. This update supersedes older statements that the third integrated mock has not started.

- ACTIVE USER-REQUESTED DATA FREEZE: FantasyPros was fully prefetched at 2026-09-08 22:35 UTC (11 requests). Until the actual league draft finishes, only Sleeper may supply fresh data. No FantasyPros, nflverse or external fresh-news retrieval. Read `docs/DRAFT_SESSION.md`. Use `data/draft_session/1400628785413394432/board.json` (also copied to the standard board path). Its approved 12-hour snapshot window overrides ordinary source-age refresh rules without changing timestamps or live-state freshness. Do not rerun collect/refresh-alerts/build, remove the guard, change cache directories or force a refresh. `draft_session.py release` must confirm the real draft complete via central Sleeper reads. Mock completion and elapsed time do not unlock access. The frozen board supports local recalculation and separately authorized mock use.

- The user is ahadik on Sleeper. League ID: 1400628784381612032.
- Assigned-order update observed September 8, 2026 at 22:02:44 UTC: ahadik is seat 13/14. Current draft 1400628785413394432 has 14 rounds, one FLEX, five bench slots and a 120-second timer. This supersedes earlier 15-round/two-FLEX mocks. Read `docs/PRE_DRAFT_PLAN.md` and `data/strategy/pre_draft/PRE_DRAFT_PLAN.md` before the next mock. Use saved `mock_profile.json` as the setup target, then verify the new room's actual settings/seat. Seat 11 is an unclaimed roster in the complete slot mapping, not a missing draft slot. Original 9 p.m. readiness target remains. Observed Sleeper start is 9:05 p.m. Eastern. Reverify order/rules before live use. Static scenarios are not operational candidates and do not refresh player-source timestamps.
- The user is new to football and wants to maximize their chance of winning this agent-managed league. Explain decisions plainly.
- Report actions visibly in commentary. Do not rely on hidden tool output. Before you run a script, give its name and relevant command options. Explain its purpose. State whether it calls external APIs or uses cached data. State whether it changes saved files or platform state. Explain which central provider client handles API access when relevant. Never include credentials or secret-bearing arguments.
- After execution, summarize actual results, cache hits/API attempts when available, files produced or changed, and failures or remaining uncertainty. Distinguish planned actions from completed actions and verified outcomes. Explain meaningful edits and tests too. Group related operations to keep updates concise. During timed drafts use brief updates so reporting does not delay picks or queue maintenance.
- Prefer deterministic code and scripts for data retrieval, validation, scoring, calculations, simulations with recorded seeds, optimization, and repeatable operations.
- Codex orchestrates a reusable system. It must not make direct external API calls. Write or extend saved scripts/commands that execute through the appropriate central API client/controller, then run those commands and analyze their outputs. This applies to diagnostics and credential checks as well as production collection. Do not use ad hoc curl, inline Python/JavaScript HTTP calls, browser fetch, or connector tools as substitutes for project API controllers.
- Minimize one-off code execution. Recurring retrieval, inspection, transformations, validation and reporting belong in reusable modules with documented command interfaces and structured outputs. When a needed operation has no command, implement the command first rather than recreating it in each conversation. Ordinary file reads, code edits and test-runner invocations remain appropriate development tools.
- Keep all test code and test fixtures under `tests/`. Run the suite from the project root with `python3 -m unittest discover -v`. Use `python3 -m unittest tests.test_fantasypros -v` for a focused module. Tests must use simulated provider responses and temporary storage. Keep live API diagnostics separate from automatic test discovery. See `tests/README.md`.
- FantasyPros access goes through `fantasypros.get_fantasypros`.  Sleeper reads go through `sleeper.get_sleeper`. Provider clients must remain independent of draft logic. Draft commands use `draft_data.read_draft_context` and `draft_data.read_live_draft` for domain collection. Shared JSON persistence lives in `storage.save_atomic`. See `docs/DATA_ACCESS.md`. New providers need a central client before use. Keep authentication, quotas, retries, caching and error handling in the provider layer, not scattered through callers. Do not imply the simpler Sleeper client already has all FantasyPros safeguards.
- The agent chooses strategy and invokes commands. Scripts own data access, computation and durable state. Keep browser draft/queue actions within the separately authorized Computer Use workflow. Browser control is not permission to call undocumented APIs. Future MCP tools should wrap the same application services, not duplicate provider access.
- nflverse reads go through `nflverse.get_nflverse` and the shared default `data/nflverse/cache/`. Use saved `nflverse_collect.py` or `draft_history.py` commands. Read `docs/NFLVERSE.md` and `docs/NFLVERSE_RESEARCH.md` before using this data. Current depth/identity publication metadata is revalidated. Completed historical stats/snaps have a six-hour maximum metadata policy, shortened by server headers. Use `--revalidate` for immediate publication checks without redownloading unchanged assets. Reserve `--refresh` for deliberate full downloads. Inspect local budgets with `nflverse_collect.py usage`. Never bypass persistent request budgets/cooldowns. Never substitute older seasons or treat historical statistics as projections. `draft_history_analysis.py` is offline and uses checksummed snapshots. Missing IDs and ambiguous joins stay quarantined. Keep current depth observation timestamps and role freshness visible. The documented ffverse ID crosswalk is a future integration candidate, not yet wired. Republished FantasyPros rankings are not independent consensus evidence.
- Sleeper uses the default shared `data/sleeper/` cache/ledger/cooldown. Only exact user profiles may be cached (maximum five minutes). All mutable and unknown paths are live, including player records and draft picks. Never extend cache ages beyond `sleeper.cache_policy`. Use `invalidate_sleeper` after known profile changes. Do not erase cooldowns or attempt records. Live draft reads use `retries=0`. Optional `with_metadata=True` exposes actual cache/attempt evidence for reporting. Follow `docs/DATA_ACCESS.md`. Do not equate retrieval timestamps with provider freshness.
- Reserve inference for analysis, strategy, interpreting evidence, and actions that computation does not handle better. Strategic inference should consume computed analyses and consider expected opponent behavior.
- Keep source timestamps, assumptions, and calculation outputs separate from strategic judgments. Do not invent projections or substitute stale season data without explicit disclosure.
- For the initial draft, prioritize a minimal local tool with saved data and manual fallbacks over cloud infrastructure.
- Before operating any draft, and after resuming from conversation compaction or interruption, read `docs/DRAFT_RUNBOOK.md`. Its queue, turn-verification, timeout-recovery, and checkpoint procedures apply throughout the draft. Mock reports are evidence, not a target roster.
- Use `controller.py` and `docs/CONTROLLER.md` for draft supervision: start the read-only watcher, record real browser observations, run `prepare` before a selection, and reconcile pending picks. Never treat a recommended queue as applied, bypass a blocker to save time, or claim the controller itself executes browser actions. On resume re-run status. Saved readiness expires.
- Sleeper's documented public API is read-only. Do not implement undocumented account writes or evasive automation.
- Before using FantasyPros data, read `docs/FANTASYPROS.md`. Follow its endpoint/scope, identity mapping, scoring-coverage, timestamp, quota and credential guidance. Keep provider facts separate from strategy. Do not treat consensus rank spread as point variance or a suggested queue as applied. Never print `.env` or authentication headers.
- Route every FantasyPros read through `fantasypros.get_fantasypros` using the shared default cache directory. Do not bypass the persistent quota/cooldown or routinely force refresh. The client's basic response checks do not replace endpoint-specific validation or establish production entitlement.
- Milestone 1 is complete as of September 8, 2026. The user supplied FantasyPros account approval for full responses, personal/non-commercial use, 1 request/second and 500/day. See `docs/FANTASYPROS.md`. Do not reopen this as unconfirmed merely because older diagnostics predate the approval. Local limits are 400 routine / 500 hard attempts per rolling 24 hours with 1.05-second spacing. Provider reset time and remaining balance remain unknown.
- Milestone 2 was subsequently authorized and implemented. Read `docs/MILESTONE_2.md`. Use `draft_inputs.py` for central-client collection and `draft_board.py` for offline joins, scoring coverage, starter-value comparison and review exports. The board's supported subtotals are not exact league totals. Its ECR order is a baseline. Preserve quarantined identities, source timestamps and candidate expiry. All top-210 consensus players have matched IDs/projections in the initial checkpoint.
- Milestone 3 was authorized and implemented. Read `docs/MILESTONE_3.md`. `draft_strategy.py` consumes the saved board and full controller state.  `strategy_policy.json` records all uncalibrated assumptions. `draft_strategy_evaluate.py` supplies offline examples, paired full drafts and reports. Use controller `--strategy-board` for automatic local recomputation after live reads. Candidate fingerprints must match the exact draft state. Source expiry remains enforced even when calculations are reused. Injury/news/history/depth flags require analysis. No automatic weekly-to-season injury multiplier. Overrides require verified IDs and reasons. Use `--allow-mock` only in a separately verified league-less mock with appropriate settings. Synthetic replays cannot be operational exports. The third integrated mock has not started and remains separately authorized. Offline evaluation is not proof of a real winning edge.
- A/B planning is now active (`planning_mode: ab`, engine version 2). Read `docs/AB_STRATEGY.md`. `draft_ab.py` uses bounded short-gap maximin plus a discounted terminal estimate for A, and long-gap scenarios through the next A–B pair for B. Exactness is limited to capped candidate/response sets. Rank slack is an opponent plausibility proxy. `draft_ab_evaluate.py` benchmarks and compares against the previous planner. Do not call three-pick scores two-pick totals, infer calibrated odds, or silently replace an A/B failure with another strategy. The before-turn/native queue/live-state safeguards still apply. Snapshot freezing was subsequently implemented. Follow `docs/DRAFT_SESSION.md`.
- The future cloud backend must expose an authenticated MCP interface for ChatGPT access on the go. Keep computation independent of the MCP adapter, desktop session, and chat history. Follow docs/MCP_DESIGN.md. Native mobile compatibility must be verified on the user's account before claiming support.
